import React from 'react'
import ReactDOM from 'react-dom/client'
import VillaMatcher from './VillaMatcher.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <VillaMatcher />
  </React.StrictMode>,
)